# Backlog

## 验证深度

**状态：** 已于 2026-09-18 完成。

**已完成：** 发布校验现在包含受限 TOML 字段的严格校验、Skill 执行 harness 与内部引用检查。隔离安装 smoke test 逐文件比对源文件、已安装文件和 receipt 的 SHA-256，验证 `-WhatIf` 不写入、冲突保护、`-Force` 备份与替换，并在 `finally` 中删除唯一的临时测试根目录后确认其不存在。

**依赖说明：** 本机未发现可复用的 TOML/YAML 解析模块，因此校验明确限定在本工具包现有的简单字符串字段范围内；没有引入联网下载的测试依赖。
